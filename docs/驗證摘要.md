# v0.1.0 驗證摘要

本文件記錄發布前在 Windows 11 x64 實機完成的驗證。所有命令均由儲存庫根目錄執行。

## 自動化結果

- `pwsh -File .\scripts\validate.ps1`：通過。
- Release 建置：零警告、零錯誤，警告視為錯誤。
- 單元與資料整合測試：18 項全數通過，涵蓋百分比邊界、單／雙限制、稀疏通知、未知欄位、陳舊資料、設定相依關係及 96／120／144 DPI 幾何計算。
- `CodexHpBar.Core` 行覆蓋率：94.52%，高於 85% 閘門。
- NuGet 直接與遞移相依套件弱點掃描：未發現已知弱點。
- Markdown 相對連結、圖片與繁體中文用詞掃描：通過。

## 實機結果

- Codex 官方 `app-server` 握手與 `account/rateLimits/read`：通過；監測器顯示的整數剩餘比例與原始回應一致。
- 兩個 1920×1080、96 DPI 工具列：主、副螢幕均正確定位；主螢幕位於系統匣左側，副螢幕保留時鐘與狀態區安全寬度。
- `--demo single|dual|offline|low`：四種模式均可啟動、顯示並正常結束。
- 正式 self-contained 單檔 EXE 在 PATH 排除 `dotnet` 後執行 `--self-test`：通過。
- 暫存安裝、首次啟用檔案、解除安裝循環：通過，安裝目錄無殘留。
- 雙視窗連續運作 120 秒：平均 CPU 0.12%，最大工作集 57.35 MB；低於 1% 與 100 MB 門檻。

## 視覺與隱私覆核

- 像素貓咪豬素材的 alpha 範圍為 0–255，透明邊緣有效；嵌入後使用 nearest-neighbor 顯示。
- 工具列截圖只保留必要區域，不含 Email、權杖、帳號、對話內容或完整桌面。
- 所有使用者介面與 Markdown 說明均人工覆核為臺灣繁體中文；程式碼、命令、API 與檔名維持原始英文。
- LINE 截圖選取模式、工具列點擊、設定置頂、右鍵選單外部關閉及副螢幕時鐘安全區已由使用者實機確認。
- EXE 與設定視窗已嵌入像素貓咪豬圖示；版本資訊包含 `g32jcj86`、GitHub 專案網址及 2026 版權資訊。
- Visual Studio 18 Community 本機 Release 重建與文件列出的 PowerShell 流程均於發布前實際執行。
